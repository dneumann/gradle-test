
group = 'sub.adw'
version = '0.0.1-SNAPSHOT'

repositories {
	mavenCentral()
}

apply plugin: 'java'

dependencies {
	compile project(':solr-importer:api')
}

build.doFirst {
	task -> println "Begin $task.project.name"
}
build.doLast {
	task -> println "Finish $task.project.name"
}
